<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title>{% block title %}{% if title %}{{ title }}{% else %}Untitled{% endif %}{% endblock %}</title>
    <link rel="stylesheet" href="{{ 'style.css'|local('css') }}"/>
    <script type="text/javascript" src="{{ 'jquery.js'|local('js') }}" charset="UTF-8"></script>
    <script type="text/javascript" src="{{ 'webgui.js'|local('js') }}"></script>
    {% block head %}{% endblock %}
</head>
<body>
    <section id="wrapper">
        <section id="main-menu">
            <a href="#" id="logo">
                <span class="preloader">N</span>ano Core
            </a>

            <a href="/admin/settings/profile/" class="right">
                <i class="fa fa-cog"></i>
            </a>
            <a href="#" class="right">
                <i class="fa fa-user"></i>
                {{ user.username }}
            </a>
        </section>

        <section id="container">
            <!--Navigation bar-->
            <nav id="main-context">
                {% for group in menu|keys %}
                    <header>
                        <i class="fa fa-module"></i>
                        {{ group }}
                    </header>

                    {% for link in menu[group]|keys %}
                        <a href="{{ menu[group][link] }}">{{ link }}</a>
                    {% endfor %}
                {% endfor %}
            </nav>

            <!--Content box-->
            <main class="content-wrapper">
                <section id="content">
                        <header>
                            {% block header %}{% if header %}{{ header }}{% else %}{{ title }}{% endif %}{% endblock %}
                        </header>
                        {% block body %}{{ content }}{% endblock %}
                </section>
            </main>
        </section>
    </section>
</body>
</html>