{% extends 'index.twig' %}

{% block body %}
    <h1>{{ category.title }}</h1>
    <div class="content">
        {% for post in posts %}
            <h3>{{ post.title }}</h3>
        {% endfor %}
    </div>
{% endblock %}